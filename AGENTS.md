# ResearchFlow agent notes

Project rules live in `.cursor/rules/` (mirrored from the workspace root).

See the parent `AGENTS.md` and:

- `00-project.mdc` — always-on ResearchFlow architecture
- `20-ai-rag.mdc` — OpenRouter AI routes/helpers
- `30-frontend-react.mdc` — UI/state conventions
- `10-backend-python.mdc` / `40-database.mdc` — future backend only
